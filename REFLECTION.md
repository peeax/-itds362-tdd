# REFLECTION

## 1. มี​เทสต์​ทั้งหมด​กี่​ตัว​และ​โค้ด​จริง​กี่​บรรทัด​อัตราส่วน​เป็น​เท่าใด
มีทั้งหมด 6 tests, โค้ด 21 บรรทัด
อัตราส่วน = 6 : 21 

## 2. ช่วง​ที่​แถบ​เป็น​สีแดง​นาน​ที่สุด​คือ​เมื่อใด​อะไร​ทำให้​มัน​นาน
ช่วงที่แถบสีแดงนานที่สุดคือขั้นตอน A6 ซึ่งเป็นขั้นตอนการเพิ่มการทำงานของการบวกโดยใช้ Sum และ Converter เนื่องจากมีหลาย class ที่ต้องทำงานร่วมกัน จึงต้องใช้เวลาในการแก้ไขและตรวจสอบการทำงานมากกว่าขั้นตอนอื่น ๆ


## 3. หา​จุด​ที่​ใช้ Fake It หนึ่ง​จุด​ใช้ Triangulate หนึ่ง​จุด​และ​ใช้ Obvious Implementation หนึ่ง​จุด​เมื่อ​มอง​ย้อนกลับ​ไป​แต่ละครั้ง​เลือก​ได้​เหมาะสม​หรือไม่
Fake It: ใช้ใน A1 โดยกำหนดค่า 600 ลงไปโดยตรงเพื่อให้ test ผ่านก่อน ซึ่งเหมาะสมเพราะช่วยตรวจสอบว่าเทสต์ทำงานได้ถูกต้อง  
Triangulate: ใช้ใน A2 โดยเพิ่มกรณี 200 × 2 = 400 ทำให้ต้องเปลี่ยนจากค่าคงที่เป็นสูตรคำนวณจริง ซึ่งเหมาะสมเพราะช่วยให้ได้กฎที่ใช้ได้กับหลายกรณี  
Obvious Implementation: ใช้ใน A3 โดยแก้ times() ให้คืน Quantity object ใหม่ เพราะวิธีแก้ชัดเจนและไม่ซับซ้อน จึงเหมาะสมกับการใช้วิธีนี้

## 4.  ยังมี​รายการ​ใด​ค้าง​อยู่​ใน​รายการ​ทดสอบ​รายการ​ใด​ที่จะ​ลงมือทำ​จริง​และ​รายการ​ใด​ที่​ควร​ลบทิ้ง​เพราะ​เป็นการ​คาดเดา​ล่วงหน้า

รายการที่ยังค้างอยู่ได้แก่:

- 200 g + 1 oz converts the result to grams
- (200 g + 1 oz) × 2

ทั้งสองรายการควรทำต่อเพราะเป็น requirements ที่อยู่ใน test list
ตั้งแต่ต้น และขณะนี้ยังไม่มีรายการใดที่ควรลบทิ้ง
เพราะรายการที่เหลือเป็นสิ่งที่โจทย์ต้องการ


## 5. ดู​เมท็อด Quantity.reduce ถ้า​ออกแบบ​ระบบ​ทั้งหมด​บน​กระดาษ​ก่อนนักศึกษาจะ​เขียน​เมท็อด​นี้​ออกมา​หรือไม่

ถ้าให้ออกแบบระบบทั้งหมดบนกระดาษก่อน อาจจะไม่ได้เขียน method reduce เพราะในช่วงเริ่มต้นยังไม่เห็นความจำเป็นของ method นี้ จนมีการเพิ่มการบวกและการจัดการผลลัพธ์ในหน่วยที่ต้องการ การทำ TDD ทีละขั้นช่วยให้เห็นว่า method นี้จำเป็นจากการทำงานของระบบ